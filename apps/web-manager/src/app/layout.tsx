import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import '@smartpark/ui/src/app/globals.css'
import { MenuItem } from '@smartpark/util/types'
import { ApolloProvider } from '@smartpark/network/src/config/apollo'
import { SessionProvider } from '@smartpark/ui/src/components/molecules/SessionProvider'
import { ToastContainer } from '@smartpark/ui/src/components/molecules/Toast'
import { Container } from '@smartpark/ui/src/components/atoms/Container'
import { Header } from '@smartpark/ui/src/components/organisms/Header'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Autospace | Manager',
  description: 'Generated by create next app',
}

const MENUITEMS: MenuItem[] = [
  { label: 'New Garage', href: '/createGarage' },
  { label: 'Valets', href: '/valets' },
]

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <body className={`${inter.className} bg-gray-25`}>
        <SessionProvider>
          <ApolloProvider>
            <Header menuItems={MENUITEMS} />
            <Container>{children}</Container>
          </ApolloProvider>
        </SessionProvider>
        <ToastContainer />
      </body>
    </html>
  )
}